# Root lombok configuration — applies to the entire project.
# See https://projectlombok.org/features/configuration

config.stopBubbling = true

# Add @Generated annotation so JaCoCo and static-analysis tools skip generated code.
lombok.addLombokGeneratedAnnotation = true
